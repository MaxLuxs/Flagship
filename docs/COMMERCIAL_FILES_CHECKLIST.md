# Чеклист коммерческих файлов для переноса в submodule

## ✅ Уже перенесено в submodule

- [x] `flagship-server/` - весь модуль сервера
- [x] `flagship-admin-ui-compose/` - модуль админки
- [x] `flagship-shared/` - общие модели

## ⚠️ Нужно перенести в submodule

### 1. Docker Compose файлы
- [ ] `docker-compose.yml` - содержит ссылки на `flagship-server/Dockerfile`
- [ ] `docker-compose.prod.yml` - содержит ссылки на `flagship-server/Dockerfile`

**Причина:** Упоминаются в pre-push hook как защищенные, содержат конфигурацию для коммерческого сервера.

### 2. GitHub Actions workflows
- [ ] `.github/workflows/ci.yml` - содержит сборку Docker образа `flagship-server`
- [ ] `.github/workflows/release.yml` - содержит публикацию Docker образа `flagship-server`

**Причина:** Содержат логику сборки и публикации коммерческого сервера.

### 3. Документация
- [ ] `docs/ADMIN_UI_REDESIGN_PLAN.md` - детальный план редизайна админки (коммерческий)

**Причина:** Содержит детальную информацию о коммерческих модулях.

### 4. Обновить документацию
- [ ] `SERVER_PROTECTION.md` - обновить, указать что теперь используется submodule
- [ ] `docs/INTEGRATION_GUIDE.md` - проверить упоминания `flagship-admin-ui-compose` (может быть публичным)

## 📝 Файлы, которые можно оставить публичными

- `samples/flagship-sample-*/README.md` - упоминают сервер, но это примеры использования (публичные)
- `docs/INTEGRATION_GUIDE.md` - упоминает `flagship-admin-ui-compose`, но это публичная документация по интеграции
- `SERVER_PROTECTION.md` - можно оставить, но обновить информацию
