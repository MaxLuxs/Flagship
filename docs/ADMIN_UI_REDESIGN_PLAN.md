# План редизайна UI админ-панели Flagship

## 📋 Обзор

Полный план редизайна админ-панели с фокусом на:
- **Адаптивность** для всех платформ (особенно веб)
- **Современный дизайн** в стиле Firebase Remote Config
- **Единый стиль** с лэндингом проекта
- **Переиспользование компонентов** из `flagship-ui-components`
- **UX улучшения** для удобства работы

---

## 🎯 Цели и принципы

### Дизайн-принципы
1. **Material Design 3** как основа
2. **Брендовые цвета** Flagship (Green #00D687, Orange #EF7200)
3. **Адаптивность** - один код для всех платформ
4. **Консистентность** - единый стиль во всех экранах
5. **Доступность** - поддержка screen readers, keyboard navigation
6. **Производительность** - оптимизация для веба

### Референсы
- **Firebase Remote Config Console** - чистота, структурированность, информативность
- **Лэндинг Flagship** - брендовые цвета, типографика, spacing
- **Material Design 3** - компоненты, elevation, motion

---

## 🏗️ Архитектура UI

### Структура компонентов

```
flagship-admin-ui-compose/
├── commonMain/
│   ├── layout/
│   │   ├── AdminLayout.kt          # Главный layout с header/footer
│   │   ├── AdminHeader.kt           # Хедер с навигацией
│   │   ├── AdminFooter.kt           # Футер с информацией
│   │   └── AdminSidebar.kt          # Боковая панель навигации
│   ├── components/
│   │   ├── cards/
│   │   │   ├── StatCard.kt         # Карточка со статистикой
│   │   │   ├── ProjectCard.kt      # Карточка проекта (улучшенная)
│   │   │   ├── FlagCard.kt         # Карточка флага
│   │   │   └── ExperimentCard.kt  # Карточка эксперимента
│   │   ├── tables/
│   │   │   ├── DataTable.kt        # Таблица данных
│   │   │   └── SortableHeader.kt   # Сортируемый заголовок
│   │   ├── forms/
│   │   │   ├── FormField.kt        # Поле формы с валидацией
│   │   │   ├── CodeEditor.kt       # Редактор кода для JSON
│   │   │   └── TargetingEditor.kt  # Улучшенный редактор targeting
│   │   ├── feedback/
│   │   │   ├── Toast.kt            # Toast уведомления
│   │   │   ├── Snackbar.kt        # Snackbar компонент
│   │   │   └── LoadingState.kt     # Состояния загрузки
│   │   └── navigation/
│   │       ├── Breadcrumbs.kt     # Хлебные крошки
│   │       └── TabNavigation.kt   # Табы навигации
│   ├── screens/
│   │   ├── auth/
│   │   │   ├── LoginScreen.kt     # Улучшенный экран входа
│   │   │   └── RegisterScreen.kt  # Улучшенный экран регистрации
│   │   ├── dashboard/
│   │   │   └── DashboardScreen.kt # Главная панель
│   │   └── project/
│   │       └── [существующие экраны с улучшениями]
│   └── theme/
│       └── AdminTheme.kt           # Расширенная тема
```

---

## 📐 Детальный план компонентов

### 1. Layout компоненты

#### 1.1 AdminLayout.kt
**Назначение:** Главный layout с header, sidebar, content, footer

**Структура:**
```kotlin
@Composable
fun AdminLayout(
    windowSize: WindowSize,
    authToken: String?,
    currentUser: UserResponse?,
    currentScreen: Screen,
    onNavigate: (Screen) -> Unit,
    onLogout: () -> Unit,
    content: @Composable (PaddingValues) -> Unit
)
```

**Особенности:**
- Адаптивная структура:
  - **Web/Desktop (> 1200dp)**: Header + Sidebar + Content + Footer
  - **Tablet (840-1200dp)**: Header + Drawer + Content + Footer
  - **Mobile (< 840dp)**: Header + Bottom Nav + Content (footer скрыт)
- Sticky header при скролле
- Footer всегда внизу (sticky на вебе)
- Максимальная ширина контента: 1400dp (центрирование на больших экранах)

**Реализация:**
- Использует `AdaptiveNavigationScaffold` как основу
- Добавляет `AdminHeader` и `AdminFooter`
- Центрирует контент на больших экранах

#### 1.2 AdminHeader.kt
**Назначение:** Верхняя панель с логотипом, навигацией, профилем

**Структура:**
```kotlin
@Composable
fun AdminHeader(
    currentUser: UserResponse?,
    currentProject: ProjectResponse?,
    onNavigate: (Screen) -> Unit,
    onLogout: () -> Unit,
    modifier: Modifier = Modifier
)
```

**Элементы:**
- **Логотип Flagship** (слева) - кликабельный, ведет на Dashboard
- **Breadcrumbs** (если есть текущий проект)
- **Поиск** (опционально, только на desktop)
- **Уведомления** (иконка с badge, если есть)
- **Профиль меню** (справа):
  - Аватар/инициалы пользователя
  - Dropdown: Profile, Settings, Logout
- **Тема** (dark/light toggle, опционально)

**Стиль:**
- Высота: 64dp
- Sticky при скролле
- Тень: elevation 2dp
- Фон: MaterialTheme.colorScheme.surface
- Использует `BrandedTopBar` из `flagship-ui-components`

**Адаптивность:**
- Desktop: все элементы видны
- Tablet: скрыт поиск, компактный профиль
- Mobile: только логотип и профиль меню

#### 1.3 AdminFooter.kt
**Назначение:** Нижняя панель с информацией и ссылками

**Структура:**
```kotlin
@Composable
fun AdminFooter(
    modifier: Modifier = Modifier
)
```

**Элементы:**
- **Логотип Flagship** (слева, маленький)
- **Ссылки:**
  - Documentation
  - GitHub
  - Support
  - Privacy Policy
  - Terms of Service
- **Версия** (справа): "v0.1.1"
- **Copyright** (справа): "© 2024 Flagship"

**Стиль:**
- Высота: 80dp (desktop), 120dp (mobile)
- Фон: MaterialTheme.colorScheme.surfaceVariant
- Текст: MaterialTheme.colorScheme.onSurfaceVariant
- Padding: 24dp горизонтально, 16dp вертикально
- Разделитель сверху: 1dp, цвет outline

**Адаптивность:**
- Desktop: горизонтальная раскладка, все ссылки видны
- Tablet: горизонтальная, компактнее
- Mobile: вертикальная раскладка, ссылки в колонку

**Поведение:**
- Всегда внизу страницы (sticky на вебе)
- Скрывается на мобильных при скролле вниз (опционально)

#### 1.4 AdminSidebar.kt
**Назначение:** Боковая панель навигации (desktop)

**Структура:**
```kotlin
@Composable
fun AdminSidebar(
    currentScreen: Screen,
    currentProject: ProjectResponse?,
    onNavigate: (Screen) -> Unit,
    modifier: Modifier = Modifier
)
```

**Элементы:**
- **Логотип** (сверху, 32dp)
- **Навигация:**
  - Dashboard (Home icon)
  - Projects (Folder icon)
  - Settings (Settings icon)
- **Текущий проект** (если выбран):
  - Заголовок "Current Project"
  - Название проекта
  - Переключатель проектов
- **Разделитель**
- **Быстрые действия:**
  - Create Project
  - Create Flag
  - Create Experiment

**Стиль:**
- Ширина: 280dp (desktop)
- Фон: MaterialTheme.colorScheme.surface
- Активный элемент: primaryContainer фон
- Иконки: 24dp
- Spacing: 8dp между элементами
- Padding: 16dp

**Анимация:**
- Hover эффект на элементах
- Плавный переход при выборе

---

### 2. Компоненты карточек

#### 2.1 StatCard.kt
**Назначение:** Карточка со статистикой (как в Firebase)

**Структура:**
```kotlin
@Composable
fun StatCard(
    title: String,
    value: String,
    subtitle: String? = null,
    icon: ImageVector? = null,
    trend: Trend? = null, // Up/Down/Neutral
    onClick: (() -> Unit)? = null,
    modifier: Modifier = Modifier
)
```

**Элементы:**
- Иконка (слева, опционально)
- Значение (крупный текст, 32sp)
- Заголовок (title, 14sp)
- Подзаголовок (subtitle, 12sp, опционально)
- Тренд (стрелка вверх/вниз, опционально)

**Стиль:**
- Использует `BrandedCard`
- Минимальная высота: 120dp
- Hover эффект (если onClick)
- Цвет иконки: primary или secondary

**Примеры использования:**
- "Total Flags" - количество флагов
- "Active Experiments" - активные эксперименты
- "API Calls" - количество API вызовов
- "Users" - количество пользователей

#### 2.2 ProjectCard.kt (улучшенная)
**Назначение:** Карточка проекта с улучшенным дизайном

**Улучшения:**
- **Визуальная иерархия:**
  - Крупное название проекта
  - Slug под названием (меньший шрифт)
  - Описание (если есть, максимум 2 строки)
- **Статистика:**
  - Флаги: иконка + число + "flags"
  - Эксперименты: иконка + число + "experiments"
  - Визуальные индикаторы (цветные badges)
- **Действия:**
  - Hover эффект: поднятие карточки
  - Клик: переход в проект
  - Quick actions (опционально): Edit, Delete
- **Статус:**
  - Индикатор активности (зеленая точка)
  - Дата последнего обновления

**Стиль:**
- Использует `BrandedCard`
- Border при hover: primary цвет, 2dp
- Elevation: 2dp → 4dp при hover
- Padding: 20dp

#### 2.3 FlagCard.kt
**Назначение:** Карточка флага в списке

**Элементы:**
- **Заголовок:**
  - Иконка типа (bool/string/number/json)
  - Ключ флага (крупный, bold)
  - Статус (enabled/disabled badge)
- **Значение:**
  - Тип: "bool", "string", etc.
  - Значение (с подсветкой синтаксиса для JSON)
- **Метаданные:**
  - Описание (если есть)
  - Дата создания/обновления
- **Действия:**
  - Toggle switch (enable/disable)
  - Edit (иконка)
  - Delete (иконка, красная)
  - Copy key (иконка, опционально)

**Стиль:**
- Использует `BrandedCard`
- Border слева: 4dp, цвет зависит от типа
- Hover: поднятие, border highlight
- Компактный layout для списка

#### 2.4 ExperimentCard.kt
**Назначение:** Карточка эксперимента

**Элементы:**
- **Заголовок:**
  - Название эксперимента
  - Статус (Active/Paused/Completed badge)
- **Варианты:**
  - Список вариантов с весами
  - Визуальные индикаторы (progress bars)
- **Targeting:**
  - Краткое описание правил
  - Иконка targeting
- **Статистика:**
  - Участников
  - Конверсия
- **Действия:**
  - Edit
  - Pause/Resume
  - Delete

**Стиль:**
- Аналогично FlagCard
- Цвет border: secondary (оранжевый)

---

### 3. Компоненты таблиц

#### 3.1 DataTable.kt
**Назначение:** Таблица данных (как в Firebase)

**Структура:**
```kotlin
@Composable
fun <T> DataTable(
    columns: List<Column<T>>,
    data: List<T>,
    onRowClick: (T) -> Unit = {},
    sortable: Boolean = true,
    searchable: Boolean = true,
    pagination: Boolean = true,
    modifier: Modifier = Modifier
)
```

**Функциональность:**
- Сортировка по колонкам
- Поиск по всем колонкам
- Пагинация (опционально)
- Выбор строк (опционально)
- Экспорт данных (опционально)

**Стиль:**
- Заголовки: MaterialTheme.typography.titleMedium
- Строки: чередующийся фон (zebra striping)
- Hover эффект на строках
- Разделители между строками
- Sticky header при скролле

**Использование:**
- Список флагов
- Список экспериментов
- Audit log
- API keys

#### 3.2 SortableHeader.kt
**Назначение:** Сортируемый заголовок колонки

**Функциональность:**
- Иконка сортировки (↑↓)
- Клик для сортировки
- Визуальная индикация текущей сортировки

---

### 4. Компоненты форм

#### 4.1 FormField.kt
**Назначение:** Поле формы с валидацией и подсказками

**Структура:**
```kotlin
@Composable
fun FormField(
    value: String,
    onValueChange: (String) -> Unit,
    label: String,
    placeholder: String? = null,
    helperText: String? = null,
    errorText: String? = null,
    required: Boolean = false,
    leadingIcon: ImageVector? = null,
    trailingIcon: ImageVector? = null,
    modifier: Modifier = Modifier
)
```

**Функциональность:**
- Валидация в реальном времени
- Подсказки (helper text)
- Ошибки (error text, красный цвет)
- Required индикатор (*)
- Иконки (leading/trailing)

#### 4.2 CodeEditor.kt
**Назначение:** Редактор кода для JSON значений

**Функциональность:**
- Подсветка синтаксиса JSON
- Валидация JSON
- Форматирование (prettify)
- Автодополнение (опционально)
- Номера строк
- Минимальная высота: 200dp

**Реализация:**
- Использует `OutlinedTextField` с моноширинным шрифтом
- Или интеграция с внешним редактором (если доступно)

#### 4.3 TargetingEditor.kt (улучшенный)
**Назначение:** Визуальный редактор targeting правил

**Улучшения:**
- **Визуальный builder:**
  - Добавление условий через UI
  - Выбор атрибутов из списка
  - Выбор операторов (equals, contains, in, etc.)
  - Ввод значений
- **Предпросмотр JSON:**
  - Показ итогового JSON
  - Валидация
- **Примеры:**
  - Предзаполненные примеры
  - Шаблоны для частых случаев

---

### 5. Компоненты обратной связи

#### 5.1 Toast.kt
**Назначение:** Toast уведомления

**Типы:**
- Success (зеленый)
- Error (красный)
- Warning (оранжевый)
- Info (синий)

**Поведение:**
- Автоматическое скрытие через 3-5 секунд
- Позиция: top-right (desktop), top-center (mobile)
- Анимация появления/исчезновения
- Stacking (несколько toast'ов)

#### 5.2 Snackbar.kt
**Назначение:** Snackbar уведомления (Material 3)

**Функциональность:**
- Action button (опционально)
- Автоматическое скрытие
- Позиция: bottom-center

#### 5.3 LoadingState.kt
**Назначение:** Состояния загрузки

**Варианты:**
- Skeleton loaders (для списков)
- Circular progress (для кнопок)
- Linear progress (для страниц)
- Shimmer эффект

---

### 6. Компоненты навигации

#### 6.1 Breadcrumbs.kt
**Назначение:** Хлебные крошки

**Структура:**
```
Dashboard > Projects > My Project > Flags
```

**Стиль:**
- Разделитель: ">" или "/"
- Кликабельные элементы (кроме последнего)
- Компактный на мобильных

#### 6.2 TabNavigation.kt
**Назначение:** Табы навигации (как в Firebase)

**Стиль:**
- Material 3 PrimaryTabRow
- Индикатор: подчеркивание
- Активный таб: primary цвет
- Компактный на мобильных (scrollable)

---

## 🎨 Улучшения экранов

### 1. LoginScreen / RegisterScreen

#### Проблемы текущей реализации:
- Занимает весь экран на вебе
- Нет header/footer
- Простой дизайн

#### Решения:

**1.1 Адаптивный контейнер:**
```kotlin
// На вебе: центрированная карточка, не на весь экран
Box(modifier = Modifier.fillMaxSize()) {
    // Header (если не авторизован, минимальный)
    AdminHeaderMinimal()
    
    // Центрированный контент
    Column(
        modifier = Modifier
            .align(Alignment.Center)
            .widthIn(max = 480.dp)
            .padding(24.dp)
    ) {
        // Форма входа
    }
    
    // Footer
    AdminFooter()
}
```

**1.2 Улучшенный дизайн:**
- **Фон:**
  - Градиент (как в лэндинге)
  - Или паттерн (опционально)
- **Карточка:**
  - Использует `BrandedCard`
  - Elevation: 8dp
  - Padding: 32dp
  - Скругление: 16dp
- **Логотип:**
  - Крупнее (64dp)
  - Центрирован
  - Анимация появления
- **Форма:**
  - Использует `FormField`
  - Валидация в реальном времени
  - Красивые placeholder'ы
- **Кнопки:**
  - Использует `BrandedButton`
  - Full width
  - Loading state

**1.3 Дополнительные элементы:**
- "Forgot password?" ссылка
- "Sign in with Google" (опционально)
- Разделитель "OR"
- Социальные ссылки в footer

**1.4 Адаптивность:**
- **Desktop (> 840dp):**
  - Центрированная карточка 480dp
  - Header и Footer видны
  - Фон с градиентом
- **Tablet (600-840dp):**
  - Карточка занимает больше места
  - Header компактный
- **Mobile (< 600dp):**
  - Карточка почти на весь экран
  - Header минимальный
  - Footer скрыт или компактный

### 2. DashboardScreen

#### Улучшения:

**2.1 Статистика:**
- Добавить `StatCard` компоненты:
  - Total Projects
  - Total Flags
  - Active Experiments
  - API Calls (last 24h)
- Grid layout:
  - Desktop: 4 колонки
  - Tablet: 2 колонки
  - Mobile: 1 колонка

**2.2 Список проектов:**
- Использовать улучшенный `ProjectCard`
- Grid layout (адаптивный)
- Фильтры и поиск
- Сортировка

**2.3 Быстрые действия:**
- FAB для создания проекта
- Quick access к последним проектам
- Недавние изменения

**2.4 Пустое состояние:**
- Красивая иллюстрация
- Призыв к действию
- Примеры использования

### 3. FlagsScreen

#### Улучшения:

**3.1 Представление:**
- **Табы:**
  - List view (текущий)
  - Table view (новая опция)
  - Grid view (опционально)
- **Фильтры:**
  - По типу
  - По статусу (enabled/disabled)
  - По дате создания
  - По тегам (если будут добавлены)

**3.2 Действия:**
- Bulk actions (выбор нескольких флагов)
- Bulk enable/disable
- Bulk delete
- Export/Import

**3.3 Информация:**
- Статистика по типам
- График изменений (опционально)
- История изменений (опционально)

**3.4 Улучшенные карточки:**
- Использовать `FlagCard` компонент
- Компактный режим (опционально)
- Расширенный режим с деталями

### 4. ExperimentsScreen

#### Улучшения:

**4.1 Представление:**
- Аналогично FlagsScreen
- Использовать `ExperimentCard`
- Табы: Active, Paused, Completed

**4.2 Визуализация:**
- Прогресс-бары для вариантов
- Статистика конверсии
- Графики (опционально)

### 5. ProjectDetailScreen

#### Улучшения:

**5.1 Навигация:**
- Табы вместо списка:
  - Overview
  - Flags
  - Experiments
  - Analytics
  - Members
  - Settings
- Breadcrumbs: Dashboard > Project Name

**5.2 Overview таб:**
- Статистика проекта
- Недавние изменения
- Быстрые действия

**5.3 Улучшенная навигация:**
- Использовать `TabNavigation`
- Sticky tabs при скролле

---

## 🎨 Тема и стилизация

### Расширение AdminTheme

**Добавить:**
```kotlin
object AdminTheme {
    // Spacing
    val spacing1 = 4.dp
    val spacing2 = 8.dp
    val spacing3 = 12.dp
    val spacing4 = 16.dp
    val spacing6 = 24.dp
    val spacing8 = 32.dp
    
    // Elevation
    val elevation1 = 1.dp
    val elevation2 = 2.dp
    val elevation4 = 4.dp
    val elevation8 = 8.dp
    
    // Border radius
    val radiusSmall = 8.dp
    val radiusMedium = 12.dp
    val radiusLarge = 16.dp
    
    // Typography scales
    // Использовать FlagshipTypography из flagship-ui-components
}
```

### Цветовая схема

**Использовать существующие:**
- `BrandColors` из `flagship-ui-components`
- `FlagshipTheme` из `flagship-ui-components`

**Дополнительные цвета (если нужно):**
- Success: BrandColors.FlagshipGreen
- Warning: BrandColors.FlagshipOrange
- Error: BrandColors.Error
- Info: синий (Material 3)

### Типографика

**Использовать:**
- `FlagshipTypography` из `flagship-ui-components`
- Соответствует лэндингу

---

## 📱 Адаптивность

### Breakpoints

```kotlin
object Breakpoints {
    val Mobile = 600.dp      // < 600dp: Mobile
    val Tablet = 840.dp      // 600-840dp: Tablet
    val Desktop = 1200.dp    // 840-1200dp: Desktop
    val LargeDesktop = 1400.dp // > 1200dp: Large Desktop
}
```

### Стратегия адаптивности

**1. Layout:**
- Mobile: вертикальный, компактный
- Tablet: смешанный, drawer навигация
- Desktop: горизонтальный, sidebar навигация

**2. Компоненты:**
- Использовать `WindowSize` из `util/WindowSize.kt`
- Условный рендеринг на основе размера
- Адаптивные модификаторы

**3. Навигация:**
- Mobile: Bottom navigation
- Tablet: Drawer
- Desktop: Sidebar

**4. Контент:**
- Mobile: 1 колонка
- Tablet: 2 колонки
- Desktop: 3-4 колонки

**5. Spacing:**
- Mobile: меньше padding (16dp)
- Desktop: больше padding (24dp)

---

## 🚀 План реализации

### Фаза 1: Foundation (Неделя 1)
1. ✅ Создать `AdminLayout` компонент
2. ✅ Создать `AdminHeader` компонент
3. ✅ Создать `AdminFooter` компонент
4. ✅ Улучшить `AdminSidebar`
5. ✅ Расширить `AdminTheme`

### Фаза 2: Компоненты (Неделя 2)
1. ✅ Создать `StatCard`
2. ✅ Улучшить `ProjectCard`
3. ✅ Создать `FlagCard`
4. ✅ Создать `ExperimentCard`
5. ✅ Создать `DataTable`
6. ✅ Создать `FormField`
7. ✅ Улучшить `TargetingEditor`

### Фаза 3: Обратная связь (Неделя 2-3)
1. ✅ Создать `Toast` компонент
2. ✅ Создать `Snackbar` компонент
3. ✅ Создать `LoadingState` компонент
4. ✅ Интегрировать во все экраны

### Фаза 4: Навигация (Неделя 3)
1. ✅ Создать `Breadcrumbs`
2. ✅ Создать `TabNavigation`
3. ✅ Интегрировать в экраны

### Фаза 5: Экраны (Неделя 3-4)
1. ✅ Улучшить `LoginScreen` / `RegisterScreen`
2. ✅ Улучшить `DashboardScreen`
3. ✅ Улучшить `FlagsScreen`
4. ✅ Улучшить `ExperimentsScreen`
5. ✅ Улучшить `ProjectDetailScreen`
6. ✅ Улучшить остальные экраны

### Фаза 6: Полировка (Неделя 4)
1. ✅ Анимации и transitions
2. ✅ Оптимизация производительности
3. ✅ Тестирование на всех платформах
4. ✅ Доступность (a11y)
5. ✅ Документация

---

## 📝 Детальные спецификации

### AdminHeader - Детали

**Высота:** 64dp (desktop), 56dp (mobile)

**Элементы слева направо:**
1. **Логотип** (40dp от левого края)
   - BrandedLogo, размер 32dp
   - Кликабельный → Dashboard
   - Hover эффект: scale 1.05

2. **Breadcrumbs** (если есть контекст)
   - Dashboard > Project Name > Flags
   - Только на desktop/tablet
   - Компактный на mobile

3. **Поиск** (только desktop, опционально)
   - OutlinedTextField
   - Ширина: 300dp
   - Иконка поиска
   - Placeholder: "Search..."

**Элементы справа:**
1. **Уведомления** (опционально)
   - IconButton с Badge
   - Badge показывает количество
   - Dropdown с уведомлениями

2. **Профиль меню**
   - Avatar или инициалы
   - Dropdown:
     - Header: имя и email
     - Divider
     - Profile
     - Settings
     - Divider
     - Logout (красный)

3. **Theme toggle** (опционально)
   - IconButton
   - Переключает dark/light

**Стиль:**
- Фон: MaterialTheme.colorScheme.surface
- Тень: elevation 2dp
- Border bottom: 1dp, цвет outline
- Padding horizontal: 16dp

### AdminFooter - Детали

**Высота:** 80dp (desktop), 120dp (mobile)

**Структура:**
```
[Logo] [Links]                    [Version] [Copyright]
```

**Ссылки:**
- Documentation → `/docs`
- GitHub → `https://github.com/MaxLuxs/Flagship`
- Support → `/support`
- Privacy Policy → `/privacy`
- Terms → `/terms`

**Стиль:**
- Фон: MaterialTheme.colorScheme.surfaceVariant
- Текст: MaterialTheme.colorScheme.onSurfaceVariant, 12sp
- Ссылки: primary цвет при hover
- Padding: 24dp horizontal, 16dp vertical
- Разделитель сверху: 1dp

**Адаптивность:**
- Desktop: горизонтальная раскладка
- Mobile: вертикальная, ссылки в колонку

### LoginScreen - Детали

**Контейнер:**
- Максимальная ширина: 480dp
- Центрирован на странице
- Padding: 24dp

**Фон:**
- Вариант 1: Градиент (как в лэндинге)
  ```kotlin
  Brush.linearGradient(
      colors = listOf(
          BrandColors.FlagshipGreenContainer,
          BrandColors.Surface
      )
  )
  ```
- Вариант 2: Паттерн (опционально)
- Вариант 3: Простой цвет (surface)

**Карточка:**
- Использует `BrandedCard`
- Elevation: 8dp
- Padding: 32dp
- Скругление: 16dp

**Форма:**
- Логотип: 64dp, центрирован
- Заголовок: "Welcome back"
- Подзаголовок: "Sign in to manage your feature flags"
- Поля:
  - Email (OutlinedTextField)
  - Password (OutlinedTextField с PasswordVisualTransformation)
- Кнопка: "Sign In" (BrandedButton, full width)
- Ссылка: "Don't have an account? Register"

**Валидация:**
- Email: проверка формата
- Password: минимум 8 символов
- Показ ошибок под полями

**Анимации:**
- Появление карточки: fadeIn + slideIn
- Задержка между элементами: 100ms

### StatCard - Детали

**Размеры:**
- Минимальная высота: 120dp
- Ширина: flex (зависит от grid)

**Структура:**
```
[Icon]  [Value]
        [Title]
        [Subtitle] [Trend]
```

**Элементы:**
- Иконка (слева, 40dp, опционально)
- Значение (крупный текст, 32sp, bold)
- Заголовок (14sp, onSurfaceVariant)
- Подзаголовок (12sp, опционально)
- Тренд (стрелка + процент, опционально)

**Стиль:**
- Использует `BrandedCard`
- Hover: elevation 2dp → 4dp
- Border при hover: primary цвет, 2dp

**Цвета иконок:**
- По умолчанию: primary
- Или кастомный цвет через параметр

### DataTable - Детали

**Заголовок:**
- Высота: 56dp
- Фон: surfaceVariant
- Sticky при скролле
- Сортировка: иконка ↑↓

**Строки:**
- Высота: 56dp (компактная) или 72dp (комфортная)
- Zebra striping (чередующийся фон)
- Hover: highlight фон
- Клик: выбор строки

**Пагинация:**
- Внизу таблицы
- Показывает: "1-10 of 100"
- Кнопки: Previous, Next
- Выбор количества на странице

**Поиск:**
- Вверху таблицы
- Фильтрует по всем колонкам
- Debounce: 300ms

---

## 🧪 Тестирование

### Платформы для тестирования

1. **Web (JS)**
   - Chrome (desktop)
   - Firefox (desktop)
   - Safari (desktop)
   - Chrome (mobile)
   - Safari (mobile)

2. **Desktop (JVM)**
   - Windows
   - macOS
   - Linux

3. **Android**
   - Phone (разные размеры)
   - Tablet

4. **iOS**
   - iPhone (разные размеры)
   - iPad

### Чеклист тестирования

**Функциональность:**
- [ ] Все экраны открываются
- [ ] Навигация работает
- [ ] Формы валидируются
- [ ] API вызовы работают
- [ ] Ошибки обрабатываются

**Адаптивность:**
- [ ] Mobile (< 600dp)
- [ ] Tablet (600-840dp)
- [ ] Desktop (840-1200dp)
- [ ] Large Desktop (> 1200dp)

**Визуальное:**
- [ ] Цвета соответствуют бренду
- [ ] Типографика читаема
- [ ] Spacing консистентный
- [ ] Иконки правильные
- [ ] Анимации плавные

**Производительность:**
- [ ] Быстрая загрузка
- [ ] Плавный скролл
- [ ] Нет лагов при взаимодействии
- [ ] Оптимизация для веба

**Доступность:**
- [ ] Screen reader support
- [ ] Keyboard navigation
- [ ] Контрастность цветов
- [ ] Focus indicators

---

## 📚 Документация

### Что нужно задокументировать

1. **Компоненты:**
   - KDoc для всех публичных компонентов
   - Примеры использования
   - Параметры и их описания

2. **Экраны:**
   - Описание функциональности
   - Скриншоты (опционально)
   - Примеры интеграции

3. **Тема:**
   - Цветовая палитра
   - Типографика
   - Spacing система

4. **Адаптивность:**
   - Breakpoints
   - Стратегии адаптации
   - Примеры

---

## 🎯 Приоритеты

### Высокий приоритет (MVP)
1. AdminLayout с Header/Footer
2. Улучшенные LoginScreen/RegisterScreen
3. StatCard компонент
4. Улучшенный DashboardScreen
5. Базовая адаптивность

### Средний приоритет
1. DataTable компонент
2. Улучшенные карточки (FlagCard, ExperimentCard)
3. Toast/Snackbar компоненты
4. Breadcrumbs и TabNavigation
5. Улучшенные экраны (Flags, Experiments)

### Низкий приоритет (Nice to have)
1. CodeEditor компонент
2. Графики и визуализации
3. Расширенные фильтры
4. Bulk actions
5. Экспорт/импорт данных

---

## ✅ Чеклист перед началом

- [ ] Изучить текущий код
- [ ] Понять структуру `flagship-ui-components`
- [ ] Изучить лэндинг (стили, цвета)
- [ ] Посмотреть примеры Firebase Remote Config UI
- [ ] Создать ветку для разработки
- [ ] Настроить окружение для тестирования на всех платформах

---

## 🔍 Анализ проекта и уточнения плана

### Структура проекта

**Модули:**
- `flagship-admin-ui-compose` - основной модуль админки
- `flagship-ui-components` - общие UI компоненты (BrandedCard, BrandedButton, etc.)
- `flagship-shared` - общие API модели (AdminApiModels.kt)

**Платформы:**
- Android (`androidMain`)
- iOS (`iosMain`)
- Desktop/JVM (`jvmMain`)
- Web/JS (`jsMain`)

**Expect/Actual функции:**
- `rememberWindowSize()` - получение размера окна
- `createDefaultHttpClient()` - создание HTTP клиента
- `ClipboardManager` - работа с буфером обмена

### Важные детали, которые нужно учесть

#### 1. WindowSize для Web - Реактивность

**Проблема:** Текущая реализация `rememberWindowSize()` для Web не реактивная - не обновляется при изменении размера окна.

**Решение:** Добавить обработчик `resize` события:

```kotlin
@Composable
actual fun rememberWindowSize(): WindowSize {
    var windowSize by remember { 
        mutableStateOf(
            WindowSize(
                width = window.innerWidth.dp,
                height = window.innerHeight.dp
            )
        )
    }
    
    DisposableEffect(Unit) {
        val handler: (Event) -> Unit = {
            windowSize = WindowSize(
                width = window.innerWidth.dp,
                height = window.innerHeight.dp
            )
        }
        window.addEventListener("resize", handler)
        onDispose {
            window.removeEventListener("resize", handler)
        }
    }
    
    return windowSize
}
```

**Добавить в план:** Детали реализации реактивного WindowSize для всех платформ.

#### 2. Entry Points для разных платформ

**Текущая структура:**
- **Android:** `MainActivity.kt` - Activity с Compose
- **iOS:** `MainViewController.kt` - UIViewController
- **Desktop:** `Main.kt` - Compose Desktop application
- **Web:** `Main.kt` - Compose Web renderComposable

**Детали для плана:**
- Описать настройку API URL для каждой платформы
- Описать размеры окон по умолчанию
- Описать интеграцию с сервером (web build копируется в server resources)

#### 3. Web Build Integration

**Текущая реализация:**
- Gradle task `copyWebBuildToServer` копирует JS build в `flagship-server/src/main/resources/admin-ui`
- Web приложение доступно по `/admin/` на сервере

**Добавить в план:**
- Детали процесса сборки и деплоя
- Настройка API URL для production
- Оптимизация bundle size для веба

#### 4. Навигация через Screen Sealed Class

**Текущая структура:**
```kotlin
sealed class Screen {
    data object Login : Screen()
    data object Register : Screen()
    data object Dashboard : Screen()
    data class ProjectDetail(val projectId: String) : Screen()
    // ...
}
```

**Добавить в план:**
- Как интегрировать новые экраны в навигацию
- Как передавать параметры между экранами
- Deep linking (опционально)

#### 5. API Models из flagship-shared

**Текущие модели:**
- `AuthResponse`, `UserResponse`
- `ProjectResponse`, `CreateProjectRequest`
- `FlagResponse`, `RestFlagValue`
- `ExperimentResponse`, `CreateExperimentRequest`
- `ApiKeyResponse`, `CreateApiKeyRequest`

**Добавить в план:**
- Убедиться что все модели учтены в UI компонентах
- Валидация данных от API
- Обработка ошибок API

#### 6. Платформо-специфичные особенности

**Android:**
- `LocalConfiguration` для WindowSize
- `Context` для ClipboardManager
- AndroidManifest.xml настройки

**iOS:**
- UIViewController интеграция
- UIPasteboard для ClipboardManager
- Размеры экрана через UIKit

**Desktop:**
- WindowState настройки
- Размеры окна по умолчанию (1200x800dp)
- Системные свойства для API URL

**Web:**
- `window.location.origin` для API URL
- HTML шаблон (index.html)
- CSS стили (если нужны)
- Bundle optimization

**Добавить в план:**
- Детали для каждой платформы
- Особенности реализации компонентов
- Тестирование на каждой платформе

#### 7. Зависимости

**Текущие зависимости:**
- `flagship-shared` - API модели
- `flagship-ui-components` - UI компоненты
- Compose Multiplatform
- Material 3
- Ktor Client
- Kotlinx Serialization

**Добавить в план:**
- Убедиться что все зависимости учтены
- Версии библиотек
- Совместимость между модулями

#### 8. Существующие экраны

**Текущие экраны:**
- `LoginScreen`, `RegisterScreen` ✅
- `DashboardScreen` ✅
- `ProjectDetailScreen` ✅
- `FlagsScreen` ✅
- `ExperimentsScreen` ✅
- `ApiKeysScreen` ✅
- `ProjectMembersScreen` ✅
- `AnalyticsScreen` ✅
- `AuditLogScreen` ✅
- `ProvidersScreen` ✅
- `ProfileSettingsScreen` ✅

**Добавить в план:**
- Детальный план улучшения каждого экрана
- Приоритеты улучшений
- Миграционный план (как обновлять существующие экраны)

#### 9. Диалоги и формы

**Текущие диалоги:**
- `CreateFlagDialog`, `EditFlagDialog`
- `CreateExperimentDialog`, `EditExperimentDialog`
- `CreateProjectDialog`
- `TargetingEditor`

**Добавить в план:**
- Улучшение всех диалогов
- Единый стиль форм
- Валидация
- Accessibility

#### 10. Состояния и ошибки

**Текущая реализация:**
- `isLoading`, `errorMessage`, `successMessage` в каждом экране
- Ручная обработка ошибок

**Добавить в план:**
- Централизованная система состояний
- Единый обработчик ошибок
- Toast/Snackbar для уведомлений
- Retry механизмы

---

## 📋 Дополнения к плану

### Раздел: Платформо-специфичные реализации

#### WindowSize - Реактивная реализация

**Web (JS):**
```kotlin
@Composable
actual fun rememberWindowSize(): WindowSize {
    var windowSize by remember { 
        mutableStateOf(
            WindowSize(
                width = window.innerWidth.dp,
                height = window.innerHeight.dp
            )
        )
    }
    
    DisposableEffect(Unit) {
        val handler: (Event) -> Unit = {
            windowSize = WindowSize(
                width = window.innerWidth.dp,
                height = window.innerHeight.dp
            )
        }
        window.addEventListener("resize", handler)
        onDispose {
            window.removeEventListener("resize", handler)
        }
    }
    
    return windowSize
}
```

**Android:**
- Использовать `LocalConfiguration.current`
- Реактивно обновляется при изменении конфигурации

**Desktop:**
- Использовать `LocalWindowSize` из Compose Desktop
- Реактивно обновляется при изменении размера окна

**iOS:**
- Использовать UIKit размеры
- Обновлять при изменении view

#### Entry Points

**Android:**
- `MainActivity.kt` - настройка темы, API URL
- AndroidManifest.xml - permissions, theme

**Desktop:**
- `Main.kt` - размеры окна по умолчанию (1200x800dp)
- Window title: "Flagship Admin"
- API URL из системных свойств

**Web:**
- `Main.kt` - автоматическое определение API URL
- `index.html` - базовый HTML шаблон
- CSS стили (если нужны)

**iOS:**
- `MainViewController.kt` - UIViewController обертка
- SwiftUI интеграция

### Раздел: Web Build и деплой

**Процесс сборки:**
1. `./gradlew :flagship-admin-ui-compose:jsBrowserProductionWebpack`
2. Gradle task `copyWebBuildToServer` копирует в server resources
3. Server отдает по `/admin/` пути

**Оптимизации:**
- Code splitting
- Tree shaking
- Minification
- Source maps для debugging

**API URL:**
- Production: автоматически из `window.location.origin`
- Development: можно переопределить

### Раздел: Навигация

**Структура:**
- `Screen` sealed class для type-safe навигации
- `AdminNavigation` composable для роутинга
- Параметры через data classes

**Улучшения:**
- Breadcrumbs на основе Screen hierarchy
- Deep linking (опционально)
- Navigation state persistence (опционально)

### Раздел: Состояния и ошибки

**Централизованная система:**
```kotlin
sealed class UiState<out T> {
    data object Loading : UiState<Nothing>()
    data class Success<T>(val data: T) : UiState<T>()
    data class Error(val message: String, val retry: () -> Unit) : UiState<Nothing>()
}
```

**Использование:**
- В каждом экране использовать `UiState`
- Единый обработчик ошибок
- Автоматический retry

### Раздел: Приоритеты улучшений экранов

**Высокий приоритет:**
1. LoginScreen/RegisterScreen - адаптивность для веба
2. DashboardScreen - статистика, улучшенные карточки
3. FlagsScreen - улучшенные карточки, фильтры
4. ProjectDetailScreen - современные табы

**Средний приоритет:**
5. ExperimentsScreen - визуализация
6. ApiKeysScreen - улучшенная таблица
7. AnalyticsScreen - графики (если нужны)

**Низкий приоритет:**
8. AuditLogScreen - улучшенная таблица
9. ProjectMembersScreen - улучшенная таблица
10. ProvidersScreen - улучшенный UI

---

## ✅ Финальный чеклист полноты плана

### Архитектура
- [x] Структура компонентов
- [x] Layout компоненты (Header, Footer, Sidebar)
- [x] Навигация
- [x] Тема и стилизация

### Компоненты
- [x] Карточки (StatCard, ProjectCard, FlagCard, ExperimentCard)
- [x] Таблицы (DataTable)
- [x] Формы (FormField, CodeEditor, TargetingEditor)
- [x] Обратная связь (Toast, Snackbar, LoadingState)
- [x] Навигация (Breadcrumbs, TabNavigation)

### Платформы
- [x] Адаптивность (breakpoints, responsive design)
- [ ] Платформо-специфичные реализации (WindowSize, HttpClient, ClipboardManager)
- [ ] Entry points для всех платформ
- [ ] Web build и деплой

### Экраны
- [x] LoginScreen/RegisterScreen
- [x] DashboardScreen
- [x] ProjectDetailScreen
- [x] FlagsScreen
- [x] ExperimentsScreen
- [ ] Остальные экраны (детали)

### Интеграция
- [x] API клиент
- [x] Модели данных
- [ ] Обработка ошибок
- [ ] Состояния загрузки
- [ ] Retry механизмы

### Детали
- [x] Иконка приложения
- [x] Selection states
- [x] Табы
- [x] Централизация констант
- [x] Детальные спецификации компонентов

---

## 📞 Вопросы для уточнения

1. Нужна ли поддержка dark theme?
2. Нужна ли интеграция с внешними сервисами (Google Auth, etc.)?
3. Нужны ли графики и визуализации данных?
4. Нужна ли поддержка мультиязычности (i18n)?
5. Какие платформы приоритетны для первой версии?

---

## 🎨 Детальные спецификации компонентов

### Иконка приложения

**Источник:** `docs/images/flagship_icon.svg` или `flagship-server/src/main/resources/landing/flagship_icon.svg`

**Реализация:**
```kotlin
// В flagship-ui-components добавить:
@Composable
fun FlagshipIcon(
    modifier: Modifier = Modifier,
    size: Dp = 32.dp,
    tint: Color? = null // Если null, использует оригинальные цвета
)
```

**Использование:**
- В `BrandedLogo` - заменить текстовый логотип на SVG иконку
- В `AdminHeader` - использовать вместо текста
- В `AdminSidebar` - использовать вверху
- В `AdminFooter` - маленькая версия (24dp)

**Цвета:**
- Зеленый флаг: `#00D687` (BrandColors.FlagshipGreen)
- Оранжевый акцент: `#EF7200` (BrandColors.FlagshipOrange)
- Фон: `#FFFEFC` (белый)

**Размеры:**
- Header: 32dp
- Sidebar: 40dp
- Footer: 24dp
- Login screen: 64dp
- Favicon: 16dp (для веба)

---

### Исправление Selection States

#### Проблема
Текущая реализация `SidebarNavigationItem` создает квадратную подсветку шире чем кнопка из-за двойного padding.

#### Решение

**Правильная структура:**
```kotlin
@Composable
private fun SidebarNavigationItem(
    selected: Boolean,
    onClick: () -> Unit,
    icon: ImageVector,
    label: String
) {
    // ВАЖНО: background ДО padding, чтобы подсветка была точно по размеру контента
    Row(
        modifier = Modifier
            .fillMaxWidth()
            .background(
                color = if (selected) 
                    MaterialTheme.colorScheme.primaryContainer 
                else 
                    Color.Transparent,
                shape = RoundedCornerShape(12.dp) // Скругление 12dp
            )
            .clickable(onClick = onClick)
            .padding(horizontal = 16.dp, vertical = 12.dp), // Padding внутри background
        verticalAlignment = Alignment.CenterVertically,
        horizontalArrangement = Arrangement.spacedBy(12.dp)
    ) {
        Icon(
            imageVector = icon,
            contentDescription = label,
            modifier = Modifier.size(24.dp),
            tint = if (selected) 
                MaterialTheme.colorScheme.onPrimaryContainer 
            else 
                MaterialTheme.colorScheme.onSurfaceVariant
        )
        Text(
            text = label,
            style = MaterialTheme.typography.labelLarge,
            fontWeight = if (selected) FontWeight.SemiBold else FontWeight.Normal,
            color = if (selected) 
                MaterialTheme.colorScheme.onPrimaryContainer 
            else 
                MaterialTheme.colorScheme.onSurfaceVariant
        )
    }
}
```

**Ключевые моменты:**
1. `background` ПЕРЕД `clickable` и `padding`
2. Скругление: `RoundedCornerShape(12.dp)` - не квадратное!
3. Padding: `horizontal = 16.dp, vertical = 12.dp` - внутри background
4. Нет двойного padding
5. Высота элемента: ~48dp (12dp * 2 + 24dp иконка)

**Адаптивность:**
- Desktop: полная ширина sidebar (280dp)
- Tablet: аналогично
- Mobile: не используется (используется bottom nav)

---

### Современные табы (TabNavigation)

#### Дизайн

**Стиль:** Material 3 PrimaryTabRow с кастомным индикатором

**Структура:**
```kotlin
@Composable
fun ModernTabRow(
    tabs: List<TabItem>,
    selectedTabIndex: Int,
    onTabSelected: (Int) -> Unit,
    modifier: Modifier = Modifier,
    scrollable: Boolean = false // Для мобильных
)
```

**TabItem:**
```kotlin
data class TabItem(
    val label: String,
    val icon: ImageVector? = null,
    val badge: Int? = null, // Количество для badge
    val enabled: Boolean = true
)
```

#### Визуальные характеристики

**Контейнер:**
- Высота: 48dp
- Фон: `MaterialTheme.colorScheme.surface`
- Border bottom: 1dp, цвет `outlineVariant`
- Padding horizontal: 0dp (табы на всю ширину)

**Таб (не выбран):**
- Высота: 48dp
- Padding horizontal: 16dp (desktop), 12dp (mobile)
- Padding vertical: 12dp
- Цвет текста: `onSurfaceVariant`
- Цвет иконки: `onSurfaceVariant`
- Typography: `titleSmall` (14sp, medium weight)
- Hover эффект: легкое изменение цвета (desktop)

**Таб (выбран):**
- Цвет текста: `primary`
- Цвет иконки: `primary`
- Typography: `titleSmall` (14sp, **semiBold weight**)
- Индикатор: подчеркивание снизу

**Индикатор (подчеркивание):**
- Высота: 3dp
- Цвет: `primary`
- Скругление: `RoundedCornerShape(topStart = 3.dp, topEnd = 3.dp)`
- Анимация: плавное перемещение при смене таба
- Ширина: равна ширине текста таба + padding (не на всю ширину таба!)

**Иконки:**
- Размер: 20dp
- Spacing от текста: 8dp
- Позиция: слева от текста
- Опционально: можно только иконка (без текста) на мобильных

**Badge:**
- Размер: 18dp диаметр
- Фон: `primary`
- Цвет текста: `onPrimary`
- Typography: `labelSmall` (11sp)
- Позиция: справа от текста, сверху
- Spacing: 4dp от текста

**Адаптивность:**

**Desktop (> 840dp):**
- Все табы видны
- Горизонтальная раскладка
- Hover эффекты
- Padding: 16dp

**Tablet (600-840dp):**
- Все табы видны (если помещаются)
- Или scrollable если много табов
- Padding: 12dp

**Mobile (< 600dp):**
- Scrollable горизонтально
- Компактные табы (можно только иконки)
- Padding: 12dp
- Swipe для переключения (опционально)

**Пример использования:**
```kotlin
ModernTabRow(
    tabs = listOf(
        TabItem("Flags", Icons.Default.Flag),
        TabItem("Experiments", Icons.Default.Science, badge = 3),
        TabItem("Settings", Icons.Default.Settings)
    ),
    selectedTabIndex = selectedTab,
    onTabSelected = { selectedTab = it }
)
```

**Анимации:**
- Переключение таба: плавное перемещение индикатора (300ms)
- Изменение цвета текста: fade transition (200ms)
- Badge появление: scale animation

---

### Централизация иконок и заголовков

#### Проблема
Повторяющиеся заголовки и иконки в разных экранах создают несогласованность.

#### Решение: Создать константы

**Файл:** `flagship-admin-ui-compose/src/commonMain/kotlin/io/maxluxs/flagship/admin/ui/compose/constants/AdminConstants.kt`

```kotlin
object AdminConstants {
    // Иконки для навигации
    object Icons {
        val Dashboard = Icons.Default.Dashboard
        val Projects = Icons.Default.Folder
        val Flags = Icons.Default.Flag
        val Experiments = Icons.Default.Science
        val ApiKeys = Icons.Default.Key
        val Members = Icons.Default.People
        val Analytics = Icons.Default.Analytics
        val Settings = Icons.Default.Settings
        val AuditLog = Icons.Default.History
        val Providers = Icons.Default.Storage
    }
    
    // Заголовки экранов
    object Titles {
        const val Dashboard = "Dashboard"
        const val Projects = "Projects"
        const val Flags = "Flags"
        const val Experiments = "Experiments"
        const val ApiKeys = "API Keys"
        const val Members = "Members"
        const val Analytics = "Analytics"
        const val Settings = "Settings"
        const val AuditLog = "Audit Log"
        const val Providers = "Providers"
    }
    
    // Описания для accessibility
    object ContentDescriptions {
        const val Dashboard = "Dashboard"
        const val Projects = "Projects"
        const val Flags = "Feature Flags"
        const val Experiments = "Experiments"
        const val ApiKeys = "API Keys"
        const val Members = "Project Members"
        const val Analytics = "Analytics"
        const val Settings = "Settings"
        const val AuditLog = "Audit Log"
        const val Providers = "Providers"
        const val Back = "Back"
        const val Menu = "Menu"
        const val Search = "Search"
        const val Notifications = "Notifications"
        const val Profile = "Profile"
        const val Logout = "Logout"
    }
}
```

**Использование:**
- Все экраны используют `AdminConstants.Titles.Flags` вместо хардкода
- Все иконки используют `AdminConstants.Icons.Flags`
- Все contentDescription используют `AdminConstants.ContentDescriptions.Flags`

---

## 📐 Максимально детальные спецификации

### 1. AdminHeader - Полная спецификация

#### Размеры и позиционирование

**Высота:**
- Desktop: 64dp
- Tablet: 64dp
- Mobile: 56dp

**Padding:**
- Horizontal: 16dp (desktop), 12dp (tablet), 8dp (mobile)
- Vertical: 0dp (высота фиксированная)

**Elevation:**
- 2dp (Material elevation)
- Тень: `md-elevation-2` из лэндинга

**Border:**
- Bottom: 1dp
- Цвет: `outlineVariant`
- Только снизу

#### Элементы слева направо

**1. Логотип (40dp от левого края)**
- Компонент: `FlagshipIcon` (SVG)
- Размер: 32dp (desktop), 28dp (mobile)
- Кликабельный: да
- Hover: scale 1.05, transition 200ms
- Spacing справа: 12dp

**2. Breadcrumbs (если есть контекст)**
- Показывать только если: `currentProject != null`
- Формат: "Dashboard > Project Name > Current Tab"
- Typography: `bodyMedium` (14sp)
- Цвет: `onSurfaceVariant`
- Разделитель: ">" или "/", цвет `outline`
- Кликабельные элементы (кроме последнего)
- Spacing: 8dp между элементами
- Скрывать на mobile если не помещается

**3. Поиск (только desktop, опционально)**
- Показывать только если: `windowSize.isExpanded && showSearch`
- Компонент: `OutlinedTextField`
- Ширина: 300dp (desktop), 240dp (tablet)
- Высота: 40dp
- Placeholder: "Search..."
- Иконка: `Icons.Default.Search`
- Spacing слева: 24dp

#### Элементы справа

**1. Уведомления (опционально)**
- Компонент: `IconButton`
- Размер: 40dp
- Иконка: `Icons.Default.Notifications`
- Badge: показывать если `notificationCount > 0`
  - Размер badge: 18dp
  - Цвет: `error`
  - Позиция: top-right иконки
- Dropdown при клике:
  - Максимальная ширина: 360dp
  - Максимальная высота: 400dp
  - Scrollable если много уведомлений
  - Каждое уведомление: высота 72dp
- Spacing слева: 8dp

**2. Профиль меню**
- Компонент: `IconButton` с dropdown
- Размер: 40dp
- Avatar:
  - Если есть фото: `AsyncImage` 32dp
  - Если нет: инициалы в `Surface` 32dp
  - Цвет фона: `primaryContainer`
  - Цвет текста: `onPrimaryContainer`
- Dropdown меню:
  - Ширина: 240dp
  - Padding: 8dp
  - Элементы:
    - Header: имя и email (padding 16dp)
    - Divider
    - "Profile" (Icon + Text, padding 12dp)
    - "Settings" (Icon + Text, padding 12dp)
    - Divider
    - "Logout" (Icon + Text, padding 12dp, цвет `error`)
- Spacing слева: 8dp

**3. Theme toggle (опционально)**
- Компонент: `IconButton`
- Размер: 40dp
- Иконка: `Icons.Default.LightMode` / `Icons.Default.DarkMode`
- Spacing слева: 8dp

#### Адаптивность

**Desktop (> 1200dp):**
- Все элементы видны
- Поиск виден
- Breadcrumbs полные

**Tablet (840-1200dp):**
- Поиск скрыт или компактный
- Breadcrumbs сокращенные
- Уведомления и профиль видны

**Mobile (< 840dp):**
- Только логотип и профиль
- Breadcrumbs скрыты
- Поиск скрыт
- Высота: 56dp

---

### 2. AdminSidebar - Полная спецификация

#### Размеры

**Ширина:**
- Desktop: 280dp (фиксированная)
- Tablet: 240dp (если используется)

**Высота:**
- `fillMaxHeight()` - на всю высоту экрана

**Фон:**
- `MaterialTheme.colorScheme.surface`
- Border right: 1dp, цвет `outlineVariant`

#### Структура (сверху вниз)

**1. Логотип (верх)**
- Компонент: `FlagshipIcon`
- Размер: 40dp
- Padding: 20dp сверху, 20dp слева
- Spacing снизу: 32dp

**2. Навигация**
- Spacing между элементами: 4dp
- Padding horizontal: 16dp

**Элемент навигации:**
- Высота: 48dp
- Padding: `horizontal = 16.dp, vertical = 12.dp`
- Background: `primaryContainer` если selected, иначе `Transparent`
- Скругление: `RoundedCornerShape(12.dp)` - **ВАЖНО: не квадратное!**
- Hover: легкое изменение цвета (desktop)
- Структура:
  - Иконка: 24dp, spacing 12dp
  - Текст: `labelLarge`, spacing 0dp

**3. Разделитель**
- Высота: 1dp
- Цвет: `outlineVariant`
- Margin: 16dp сверху и снизу

**4. Текущий проект (если выбран)**
- Заголовок: "Current Project"
  - Typography: `labelSmall` (11sp)
  - Цвет: `onSurfaceVariant`
  - Padding: 16dp horizontal, 8dp сверху
- Название проекта:
  - Typography: `titleMedium` (16sp, semiBold)
  - Цвет: `onSurface`
  - Padding: 16dp horizontal, 4dp снизу
- Кнопка "Switch Project":
  - Компонент: `OutlinedButton`
  - Размер: compact
  - Padding: 16dp horizontal

**5. Быстрые действия (опционально)**
- Заголовок: "Quick Actions"
  - Typography: `labelSmall`
  - Padding: 16dp horizontal, 16dp сверху
- Кнопки:
  - "Create Project" (Icon + Text)
  - "Create Flag" (Icon + Text)
  - "Create Experiment" (Icon + Text)
  - Каждая: высота 40dp, padding 12dp

**6. Spacer** (внизу для footer, если нужен)

#### Адаптивность

**Desktop:**
- Всегда виден
- Фиксированная ширина 280dp

**Tablet:**
- В drawer (скрыт по умолчанию)
- Ширина 240dp

**Mobile:**
- Не используется (используется bottom nav)

---

### 3. AdminFooter - Полная спецификация

#### Размеры

**Высота:**
- Desktop: 80dp
- Tablet: 80dp
- Mobile: 120dp (вертикальная раскладка)

**Padding:**
- Horizontal: 24dp (desktop), 16dp (mobile)
- Vertical: 16dp

#### Структура

**Desktop/Tablet (горизонтальная):**

```
[Logo 24dp] [Links]                    [Version] [Copyright]
```

**Mobile (вертикальная):**

```
[Logo 24dp]
[Links - вертикально]
[Version] [Copyright]
```

**Элементы:**

1. **Логотип**
   - Компонент: `FlagshipIcon`
   - Размер: 24dp
   - Spacing справа: 16dp

2. **Ссылки**
   - Typography: `bodySmall` (12sp)
   - Цвет: `onSurfaceVariant`
   - Hover: `primary` цвет
   - Spacing между ссылками: 16dp
   - Ссылки:
     - "Documentation"
     - "GitHub"
     - "Support"
     - "Privacy"
     - "Terms"

3. **Spacer** (flex weight = 1f)

4. **Версия**
   - Typography: `bodySmall` (12sp)
   - Цвет: `onSurfaceVariant`
   - Формат: "v0.1.1"
   - Spacing справа: 16dp

5. **Copyright**
   - Typography: `bodySmall` (12sp)
   - Цвет: `onSurfaceVariant`
   - Формат: "© 2024 Flagship"

#### Фон и границы

- Фон: `MaterialTheme.colorScheme.surfaceVariant`
- Border top: 1dp, цвет `outlineVariant`
- Elevation: 0dp (не нужна тень)

---

### 4. StatCard - Полная спецификация

#### Размеры

**Минимальная высота:** 120dp
**Ширина:** flex (зависит от grid)

**Padding:**
- Все стороны: 20dp

#### Структура

```
┌─────────────────────┐
│ [Icon]  [Value]     │
│         [Title]     │
│         [Subtitle]  │ [Trend]
└─────────────────────┘
```

**Элементы:**

1. **Иконка (опционально)**
   - Размер: 40dp
   - Цвет: `primary` или кастомный
   - Позиция: слева вверху
   - Spacing справа: 16dp

2. **Значение**
   - Typography: `displaySmall` (32sp, bold)
   - Цвет: `onSurface`
   - Spacing снизу: 4dp

3. **Заголовок**
   - Typography: `bodyMedium` (14sp)
   - Цвет: `onSurfaceVariant`
   - Spacing снизу: 4dp

4. **Подзаголовок (опционально)**
   - Typography: `bodySmall` (12sp)
   - Цвет: `onSurfaceVariant`

5. **Тренд (опционально)**
   - Позиция: справа вверху
   - Иконка: стрелка вверх/вниз, 16dp
   - Текст: процент, `labelMedium` (12sp)
   - Цвет:
     - Up: `success` (зеленый)
     - Down: `error` (красный)
     - Neutral: `onSurfaceVariant`

#### Стиль

- Использует `BrandedCard`
- Elevation: 2dp (default)
- Hover (desktop): elevation 4dp, border `primary` 2dp
- Скругление: 12dp

#### Адаптивность

**Grid layout:**
- Desktop (> 1200dp): 4 колонки
- Tablet (840-1200dp): 2 колонки
- Mobile (< 840dp): 1 колонка

**Spacing между карточками:**
- Desktop: 16dp
- Tablet: 12dp
- Mobile: 8dp

---

### 5. FlagCard - Полная спецификация

#### Размеры

**Высота:** auto (минимум 80dp)
**Ширина:** `fillMaxWidth()`

**Padding:**
- Все стороны: 16dp

#### Структура

```
┌─────────────────────────────────────────────┐
│ [Icon] [Key] [Status]        [Toggle] [Edit] [Delete] │
│        [Type: Value]                              │
│        [Description]                              │
└─────────────────────────────────────────────────────┘
```

**Элементы:**

1. **Иконка типа**
   - Размер: 20dp
   - Цвет зависит от типа:
     - `bool`: `FlagshipGreen`
     - `string`: `FlagshipOrange`
     - `number`: `primary`
     - `json`: `secondary`
   - Spacing справа: 8dp

2. **Ключ флага**
   - Typography: `titleMedium` (16sp, bold)
   - Цвет: `onSurface`
   - Spacing справа: 8dp

3. **Статус badge**
   - Размер: 8dp диаметр
   - Цвет: `FlagshipGreen` если enabled, иначе `onSurfaceVariant`
   - Spacing справа: 8dp

4. **Тип и значение**
   - Typography: `bodyMedium` (14sp)
   - Цвет: `onSurfaceVariant`
   - Формат: "bool: true" или "string: 'value'"
   - Spacing сверху: 4dp

5. **Описание (если есть)**
   - Typography: `bodySmall` (12sp)
   - Цвет: `onSurfaceVariant`
   - Максимум: 2 строки, ellipsis
   - Spacing сверху: 4dp

6. **Действия (справа)**
   - Toggle Switch: размер compact
   - Edit IconButton: 40dp
   - Delete IconButton: 40dp, цвет `error`
   - Spacing между: 8dp

#### Стиль

- Использует `BrandedCard`
- Border left: 4dp, цвет зависит от типа
- Elevation: 2dp
- Hover: elevation 4dp, border highlight
- Скругление: 12dp

#### Адаптивность

**Desktop:**
- Горизонтальная раскладка
- Все действия видны

**Mobile:**
- Вертикальная раскладка
- Действия внизу
- Компактный режим опционально

---

### 6. DataTable - Полная спецификация

#### Размеры

**Заголовок:**
- Высота: 56dp
- Padding horizontal: 16dp

**Строка:**
- Высота: 56dp (компактная) или 72dp (комфортная)
- Padding horizontal: 16dp

#### Структура

**Заголовок:**
- Фон: `surfaceVariant`
- Sticky при скролле
- Border bottom: 1dp, цвет `outlineVariant`
- Каждая колонка:
  - Typography: `titleSmall` (14sp, semiBold)
  - Цвет: `onSurfaceVariant`
  - Сортировка: иконка ↑↓ справа, 16dp
  - Кликабельный для сортировки

**Строка:**
- Фон: чередующийся (zebra striping)
  - Четная: `surface`
  - Нечетная: `surfaceVariant` (alpha 0.5)
- Hover: `primaryContainer` (alpha 0.3)
- Border bottom: 1dp, цвет `outlineVariant` (alpha 0.5)
- Кликабельный: да (если `onRowClick` предоставлен)

**Пагинация (внизу):**
- Высота: 56dp
- Фон: `surface`
- Border top: 1dp, цвет `outlineVariant`
- Padding: 16dp horizontal
- Элементы:
  - Текст: "1-10 of 100"
  - Кнопки: Previous, Next
  - Выбор количества: Dropdown

#### Адаптивность

**Desktop:**
- Все колонки видны
- Горизонтальный скролл если нужно

**Tablet:**
- Некоторые колонки могут быть скрыты
- Горизонтальный скролл

**Mobile:**
- Вертикальная раскладка (каждая строка = карточка)
- Или горизонтальный скролл

---

### 7. FormField - Полная спецификация

#### Размеры

**Высота:** auto (минимум 56dp для OutlinedTextField)
**Ширина:** `fillMaxWidth()` или кастомная

#### Структура

```
[Label] [Required *]
[TextField]
[Helper Text] или [Error Text]
```

**Элементы:**

1. **Label**
   - Typography: `bodyMedium` (14sp)
   - Цвет: `onSurfaceVariant`
   - Spacing снизу: 4dp
   - Required индикатор: "*", цвет `error`, spacing 4dp

2. **TextField**
   - Компонент: `OutlinedTextField`
   - Высота: 56dp
   - Скругление: 12dp
   - Border: 1dp, цвет `outline`
   - Border при focus: 2dp, цвет `primary`
   - Padding: 16dp horizontal

3. **Helper Text**
   - Typography: `bodySmall` (12sp)
   - Цвет: `onSurfaceVariant`
   - Spacing сверху: 4dp
   - Показывать если: `helperText != null && errorText == null`

4. **Error Text**
   - Typography: `bodySmall` (12sp)
   - Цвет: `error`
   - Spacing сверху: 4dp
   - Показывать если: `errorText != null`

5. **Leading Icon (опционально)**
   - Размер: 24dp
   - Цвет: `onSurfaceVariant`
   - Spacing справа: 12dp

6. **Trailing Icon (опционально)**
   - Размер: 24dp
   - Цвет: `onSurfaceVariant`
   - Spacing слева: 12dp

#### Состояния

**Default:**
- Border: `outline`
- Цвет текста: `onSurface`

**Focused:**
- Border: `primary`, 2dp
- Цвет текста: `onSurface`

**Error:**
- Border: `error`, 2dp
- Цвет текста: `onSurface`
- Показывать error text

**Disabled:**
- Border: `outlineVariant`
- Цвет текста: `onSurfaceVariant` (alpha 0.6)
- Фон: `surfaceVariant`

---

### 8. Toast - Полная спецификация

#### Размеры

**Ширина:** 320dp (desktop), `fillMaxWidth() - 32dp` (mobile)
**Высота:** auto (минимум 48dp)

**Padding:**
- Horizontal: 16dp
- Vertical: 12dp

#### Структура

```
[Icon] [Message]        [Close]
```

**Элементы:**

1. **Иконка**
   - Размер: 24dp
   - Цвет зависит от типа:
     - Success: `success`
     - Error: `error`
     - Warning: `warning`
     - Info: `info`
   - Spacing справа: 12dp

2. **Сообщение**
   - Typography: `bodyMedium` (14sp)
   - Цвет: `onSurface`
   - Flex weight: 1f

3. **Кнопка закрытия (опционально)**
   - Размер: 24dp
   - Иконка: `Icons.Default.Close`
   - Spacing слева: 12dp

#### Стиль

- Фон: `surface` (elevation 6dp)
- Скругление: 12dp
- Border: 1dp, цвет `outlineVariant`

#### Позиционирование

**Desktop:**
- Top-right
- Spacing от края: 16dp
- Z-index: 1000

**Mobile:**
- Top-center
- Spacing от края: 16dp
- Z-index: 1000

#### Анимации

**Появление:**
- Slide in справа (desktop) или сверху (mobile)
- Fade in
- Длительность: 300ms

**Исчезновение:**
- Slide out
- Fade out
- Длительность: 200ms

**Автоматическое скрытие:**
- Success: 3 секунды
- Error: 5 секунд
- Warning: 4 секунды
- Info: 3 секунды

#### Stacking

- Максимум: 3 toast'а одновременно
- Spacing между: 8dp
- Старые скрываются при добавлении новых (если > 3)

---

## 🎯 Чеклист исправлений

### Критичные исправления

- [ ] **Selection states:** Исправить подсветку в `SidebarNavigationItem` - убрать квадратную, сделать скругленную
- [ ] **Иконка приложения:** Добавить SVG иконку вместо текстового логотипа
- [ ] **Повторяющиеся заголовки:** Создать `AdminConstants` и использовать везде
- [ ] **Табы:** Улучшить дизайн табов с правильным индикатором
- [ ] **Адаптивность LoginScreen:** Сделать центрированную карточку на вебе

### Улучшения

- [ ] Добавить все детальные спецификации в код
- [ ] Протестировать на всех платформах
- [ ] Оптимизировать производительность
- [ ] Добавить анимации везде где нужно

---

**Дата создания:** 2024
**Версия плана:** 2.0
**Статус:** Детализированный, готов к реализации
