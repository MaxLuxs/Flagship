# Админ-панель: Что осталось доделать

## ✅ Что уже реализовано

1. **Login / Register** ✅
   - Вход по email + пароль
   - Регистрация пользователей
   - JWT токены в localStorage
   - Logout

2. **Dashboard** ✅
   - Список проектов
   - Создание проекта
   - Выбор проекта

3. **Флаги (базовое)** ✅
   - Список флагов
   - Создание флага
   - Удаление флага

4. **Эксперименты (базовое)** ✅
   - Список экспериментов
   - Удаление эксперимента

5. **API Ключи** ✅
   - Список API ключей
   - Создание API ключа

---

## ❌ Что нужно доделать

### 🔴 Критично для MVP

#### 1. Флаги - Toggle on/off
**Проблема**: API возвращает только `RestFlagValue` (type, value), но не метаданные (isEnabled, description, dates)

**Решение**:
- [ ] Добавить endpoint `GET /api/projects/{id}/flags` который возвращает полную информацию о флагах
- [ ] Или создать отдельный endpoint `GET /api/projects/{id}/flags-meta` для метаданных
- [ ] Добавить endpoint `PATCH /api/projects/{id}/flags/{key}/toggle` для быстрого переключения
- [ ] В UI: добавить toggle switch в таблицу флагов
- [ ] Обновлять статус без перезагрузки страницы

**API изменения:**
```kotlin
// Новый endpoint для toggle
patch("{key}/toggle") {
    // Переключить isEnabled
}

// Или расширить GET flags чтобы возвращать метаданные
```

#### 2. Редактирование флага
- [ ] Кнопка "Редактировать" в таблице
- [ ] Модальное окно с предзаполненными полями (key, type, value, description)
- [ ] Использовать `PUT /api/projects/{id}/flags/{key}`
- [ ] Обновление description через API

#### 3. Создание эксперимента (полная форма)
**Текущее состояние**: Только `alert('Создание экспериментов будет добавлено в следующей версии')`

**Нужно**:
- [ ] Модальное окно с формой:
  - Ключ (key)
  - Описание (description)
  - Варианты (variants):
    - Динамическое добавление вариантов
    - Для каждого: name, weight
    - Валидация: сумма весов = 1.0
  - Сегментация (опционально):
    - Страны (список)
    - Версия приложения (min/max)
- [ ] Отправка через `POST /api/projects/{id}/experiments`

#### 4. Редактирование эксперимента
- [ ] Кнопка "Редактировать" в таблице
- [ ] Модальное окно с предзаполненными данными
- [ ] Использовать `PUT /api/projects/{id}/experiments/{key}`

#### 5. Показ email пользователя
- [ ] После логина сохранять email в переменную
- [ ] Отображать в `#user-email` элементе

### 🟡 Важно для MVP

#### 6. Метаданные флагов в API
**Проблема**: `getAllFlags()` возвращает только `Map<String, RestFlagValue>`, но не метаданные

**Решение**:
- [ ] Создать DTO `FlagWithMetadata` с полями:
  - key, type, value (RestFlagValue)
  - isEnabled, description, createdAt, updatedAt
- [ ] Новый endpoint `GET /api/projects/{id}/flags-detailed` или расширить существующий
- [ ] Обновить UI для отображения метаданных

#### 7. Аналитика (базовая)
- [ ] Новая вкладка "Аналитика"
- [ ] График распределения пользователей по вариантам экспериментов
- [ ] Метрики: количество экспозиций флагов
- [ ] API: нужно добавить endpoints для аналитики (или использовать audit log)

#### 8. Audit Log просмотр
- [ ] Вкладка или секция в настройках проекта
- [ ] Список всех действий (кто, что, когда)
- [ ] Использовать `GET /api/admin/projects/{id}/audit`
- [ ] Фильтры: тип действия, пользователь, дата

#### 9. Удаление API ключа
- [ ] Кнопка удаления в таблице
- [ ] Подтверждение перед удалением
- [ ] API: `DELETE /api/admin/projects/{id}/api-keys/{keyId}` (нужно добавить)

#### 10. Удаление проекта
- [ ] Кнопка в настройках проекта
- [ ] Подтверждение (опасное действие)
- [ ] API: `DELETE /api/admin/projects/{id}` (нужно добавить)

#### 11. Редактирование проекта
- [ ] Кнопка редактирования в dashboard
- [ ] Модальное окно с предзаполненными данными
- [ ] API: `PUT /api/admin/projects/{id}` (нужно добавить)

### 🟢 Желательно (можно после MVP)

#### 12. Realtime обновления (SSE/WebSocket)
- [ ] SSE endpoint: `GET /api/projects/{id}/events`
- [ ] Подключение EventSource в UI
- [ ] Обработка событий: flag_changed, experiment_changed
- [ ] Автоматическое обновление таблиц
- [ ] Визуальная индикация изменений

#### 13. Сегментация при создании
- [ ] Поля сегментации в формах создания флагов/экспериментов
- [ ] Страны (мультиселект)
- [ ] Версия приложения (min/max)
- [ ] Сохранение в targeting rules

#### 14. UX улучшения
- [ ] Индикаторы загрузки (spinner)
- [ ] Обработка ошибок (понятные сообщения)
- [ ] Валидация форм на клиенте
- [ ] Сортировка таблиц
- [ ] Поиск/фильтрация

---

## Технические детали

### API endpoints, которые нужно добавить/изменить

**Флаги:**
```kotlin
// Расширенный список флагов с метаданными
GET /api/projects/{id}/flags-detailed
Response: List<FlagWithMetadata>

// Toggle флага
PATCH /api/projects/{id}/flags/{key}/toggle
Response: { "isEnabled": true/false }

// Или расширить PUT для обновления isEnabled
PUT /api/projects/{id}/flags/{key}
Body: { "type": "bool", "value": true, "isEnabled": false, "description": "..." }
```

**Проекты:**
```kotlin
PUT /api/admin/projects/{id}
Body: { "name": "...", "description": "..." }

DELETE /api/admin/projects/{id}
```

**API ключи:**
```kotlin
DELETE /api/admin/projects/{id}/api-keys/{keyId}
```

**Аналитика:**
```kotlin
GET /api/admin/projects/{id}/analytics
Response: { "flagExposures": {...}, "experimentDistribution": {...} }
```

### Компоненты для реализации

**Toggle switch (уже есть в CSS):**
```html
<label class="toggle-switch">
    <input type="checkbox" checked onchange="toggleFlag('key', this.checked)">
    <span class="toggle-slider"></span>
</label>
```

**Функция toggle:**
```javascript
async function toggleFlag(key, enabled) {
    const response = await fetch(`${API_BASE}/api/projects/${currentProjectId}/flags/${key}/toggle`, {
        method: 'PATCH',
        headers: {
            'Authorization': `Bearer ${token}`,
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({ isEnabled: enabled })
    });
    if (response.ok) {
        loadFlags(); // Обновить список
    }
}
```

---

## Приоритетный план действий

### Фаза 1: Критичные функции (4-6 часов)
1. ✅ Добавить API для метаданных флагов
2. ✅ Toggle флагов (UI + API)
3. ✅ Редактирование флага
4. ✅ Создание эксперимента (полная форма)
5. ✅ Редактирование эксперимента
6. ✅ Показ email пользователя

### Фаза 2: Важные функции (4-6 часов)
7. ✅ Audit log просмотр
8. ✅ Удаление API ключа (API + UI)
9. ✅ Удаление проекта (API + UI)
10. ✅ Редактирование проекта (API + UI)
11. ✅ Базовая аналитика

### Фаза 3: Улучшения (4-6 часов)
12. ✅ Realtime обновления
13. ✅ Сегментация
14. ✅ UX улучшения

**Итого для полного MVP: ~12-18 часов работы**
